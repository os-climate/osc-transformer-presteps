---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2024 The Linux Foundation

name: "🐍 Update Dependencies"

on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 1 * *"
  push:
    branches:
      - "main"
      - "master"
    paths:
      - "!pdm.lock"
      - "!.github/**"

env:
  python-version: 3.11

permissions: {}

jobs:
  python-dependencies:
    name: "Update Dependencies"
    # yamllint disable-line rule:line-length
    uses: os-climate/osc-github-devops/.github/workflows/reuse-dependencies.yaml@c5395f432f2b12fcdbb643e1275d3e315060ce45 # 2025-01-02
    permissions:
      # IMPORTANT: mandatory to raise the PR
      id-token: write
      pull-requests: write
      repository-projects: write
      contents: write
